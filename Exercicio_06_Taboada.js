let Number = prompt("Digite um numéro: ");
let Result;
for (let Table = 1; Table <= 20; Table++) {
  Result = Number * Table;
  prompt(Number + " x " + Table + " = " + Result);
  console.log(Result);
}
Number.length;
